# SWEII-Engagement-App
go into  folder "src" by typing "cd src" and type "nodemon app.js" in the terminal to run serve

while in root folder "CLIENTSERVERMERGE" and type "npm run serve" in terminal to run client 

